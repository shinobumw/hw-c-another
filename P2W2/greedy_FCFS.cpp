#include <bits/stdc++.h>
using namespace std;

int main()
{
	int testcase;
	cin >> testcase;
	while (testcase--) {
		int n, deadline;
		cin >> n >> deadline;
		priority_queue<int> job;
		for (int i = 0; i < n; ++i) {
			int tmp;
			job.push(tmp);
		}
	}
	return 0;	
}
